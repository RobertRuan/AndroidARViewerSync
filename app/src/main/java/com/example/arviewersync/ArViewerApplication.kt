package com.example.arviewersync

import android.app.Application

/**
 * Application class — kept minimal; future global state (DI, logger, etc.)
 * would go here. Required because the manifest references `.ArViewerApplication`.
 */
class ArViewerApplication : Application()
